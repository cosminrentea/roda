// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ro.roda.CatalogAcl;
import ro.roda.CatalogAclDataOnDemand;
import ro.roda.CatalogAclPK;
import ro.roda.CatalogDataOnDemand;

privileged aspect CatalogAclDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CatalogAclDataOnDemand: @Component;
    
    private Random CatalogAclDataOnDemand.rnd = new SecureRandom();
    
    private List<CatalogAcl> CatalogAclDataOnDemand.data;
    
    @Autowired
    private CatalogDataOnDemand CatalogAclDataOnDemand.catalogDataOnDemand;
    
    public CatalogAcl CatalogAclDataOnDemand.getNewTransientCatalogAcl(int index) {
        CatalogAcl obj = new CatalogAcl();
        setEmbeddedIdClass(obj, index);
        setDelete(obj, index);
        setModacl(obj, index);
        setRead(obj, index);
        setUpdate(obj, index);
        return obj;
    }
    
    public void CatalogAclDataOnDemand.setEmbeddedIdClass(CatalogAcl obj, int index) {
        Integer catalogId = new Integer(index);
        Integer aroId = new Integer(index);
        Integer aroType = new Integer(index);
        
        CatalogAclPK embeddedIdClass = new CatalogAclPK(catalogId, aroId, aroType);
        obj.setId(embeddedIdClass);
    }
    
    public void CatalogAclDataOnDemand.setDelete(CatalogAcl obj, int index) {
        Boolean delete = Boolean.TRUE;
        obj.setDelete(delete);
    }
    
    public void CatalogAclDataOnDemand.setModacl(CatalogAcl obj, int index) {
        Boolean modacl = Boolean.TRUE;
        obj.setModacl(modacl);
    }
    
    public void CatalogAclDataOnDemand.setRead(CatalogAcl obj, int index) {
        Boolean read = Boolean.TRUE;
        obj.setRead(read);
    }
    
    public void CatalogAclDataOnDemand.setUpdate(CatalogAcl obj, int index) {
        Boolean update = Boolean.TRUE;
        obj.setUpdate(update);
    }
    
    public CatalogAcl CatalogAclDataOnDemand.getSpecificCatalogAcl(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CatalogAcl obj = data.get(index);
        CatalogAclPK id = obj.getId();
        return CatalogAcl.findCatalogAcl(id);
    }
    
    public CatalogAcl CatalogAclDataOnDemand.getRandomCatalogAcl() {
        init();
        CatalogAcl obj = data.get(rnd.nextInt(data.size()));
        CatalogAclPK id = obj.getId();
        return CatalogAcl.findCatalogAcl(id);
    }
    
    public boolean CatalogAclDataOnDemand.modifyCatalogAcl(CatalogAcl obj) {
        return false;
    }
    
    public void CatalogAclDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CatalogAcl.findCatalogAclEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CatalogAcl' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CatalogAcl>();
        for (int i = 0; i < 10; i++) {
            CatalogAcl obj = getNewTransientCatalogAcl(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
