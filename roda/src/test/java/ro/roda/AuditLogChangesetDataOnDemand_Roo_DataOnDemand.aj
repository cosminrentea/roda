// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ro.roda.AuditLogChangeset;
import ro.roda.AuditLogChangesetDataOnDemand;
import ro.roda.Rodauser;
import ro.roda.RodauserDataOnDemand;

privileged aspect AuditLogChangesetDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AuditLogChangesetDataOnDemand: @Component;
    
    private Random AuditLogChangesetDataOnDemand.rnd = new SecureRandom();
    
    private List<AuditLogChangeset> AuditLogChangesetDataOnDemand.data;
    
    @Autowired
    RodauserDataOnDemand AuditLogChangesetDataOnDemand.rodauserDataOnDemand;
    
    public AuditLogChangeset AuditLogChangesetDataOnDemand.getNewTransientAuditLogChangeset(int index) {
        AuditLogChangeset obj = new AuditLogChangeset();
        setDescription(obj, index);
        setRodauser(obj, index);
        setTimestamp(obj, index);
        return obj;
    }
    
    public void AuditLogChangesetDataOnDemand.setDescription(AuditLogChangeset obj, int index) {
        String description = "description_" + index;
        if (description.length() > 255) {
            description = description.substring(0, 255);
        }
        obj.setDescription(description);
    }
    
    public void AuditLogChangesetDataOnDemand.setRodauser(AuditLogChangeset obj, int index) {
        Rodauser rodauser = rodauserDataOnDemand.getRandomRodauser();
        obj.setRodauser(rodauser);
    }
    
    public void AuditLogChangesetDataOnDemand.setTimestamp(AuditLogChangeset obj, int index) {
        Calendar timestamp = Calendar.getInstance();
        obj.setTimestamp(timestamp);
    }
    
    public AuditLogChangeset AuditLogChangesetDataOnDemand.getSpecificAuditLogChangeset(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AuditLogChangeset obj = data.get(index);
        Integer id = obj.getId();
        return AuditLogChangeset.findAuditLogChangeset(id);
    }
    
    public AuditLogChangeset AuditLogChangesetDataOnDemand.getRandomAuditLogChangeset() {
        init();
        AuditLogChangeset obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return AuditLogChangeset.findAuditLogChangeset(id);
    }
    
    public boolean AuditLogChangesetDataOnDemand.modifyAuditLogChangeset(AuditLogChangeset obj) {
        return false;
    }
    
    public void AuditLogChangesetDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AuditLogChangeset.findAuditLogChangesetEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AuditLogChangeset' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AuditLogChangeset>();
        for (int i = 0; i < 10; i++) {
            AuditLogChangeset obj = getNewTransientAuditLogChangeset(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
