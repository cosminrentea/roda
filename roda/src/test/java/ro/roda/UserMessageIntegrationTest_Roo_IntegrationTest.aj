// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.UserMessageDataOnDemand;
import ro.roda.UserMessageIntegrationTest;
import ro.roda.service.UserMessageService;

privileged aspect UserMessageIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserMessageIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserMessageIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserMessageIntegrationTest: @Transactional;
    
    @Autowired
    UserMessageDataOnDemand UserMessageIntegrationTest.dod;
    
    @Autowired
    UserMessageService UserMessageIntegrationTest.userMessageService;
    
    @Test
    public void UserMessageIntegrationTest.testCountAllUserMessages() {
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", dod.getRandomUserMessage());
        long count = userMessageService.countAllUserMessages();
        Assert.assertTrue("Counter for 'UserMessage' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserMessageIntegrationTest.testFindUserMessage() {
        UserMessage obj = dod.getRandomUserMessage();
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to provide an identifier", id);
        obj = userMessageService.findUserMessage(id);
        Assert.assertNotNull("Find method for 'UserMessage' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserMessage' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserMessageIntegrationTest.testFindAllUserMessages() {
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", dod.getRandomUserMessage());
        long count = userMessageService.countAllUserMessages();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserMessage', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserMessage> result = userMessageService.findAllUserMessages();
        Assert.assertNotNull("Find all method for 'UserMessage' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserMessage' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserMessageIntegrationTest.testFindUserMessageEntries() {
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", dod.getRandomUserMessage());
        long count = userMessageService.countAllUserMessages();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserMessage> result = userMessageService.findUserMessageEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserMessage' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserMessage' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserMessageIntegrationTest.testSaveUserMessage() {
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", dod.getRandomUserMessage());
        UserMessage obj = dod.getNewTransientUserMessage(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserMessage' identifier to be null", obj.getId());
        userMessageService.saveUserMessage(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserMessage' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserMessageIntegrationTest.testDeleteUserMessage() {
        UserMessage obj = dod.getRandomUserMessage();
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserMessage' failed to provide an identifier", id);
        obj = userMessageService.findUserMessage(id);
        userMessageService.deleteUserMessage(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserMessage' with identifier '" + id + "'", userMessageService.findUserMessage(id));
    }
    
}
