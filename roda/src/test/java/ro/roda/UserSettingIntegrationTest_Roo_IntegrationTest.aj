// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.UserSettingDataOnDemand;
import ro.roda.UserSettingIntegrationTest;
import ro.roda.service.UserSettingService;

privileged aspect UserSettingIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserSettingIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserSettingIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserSettingIntegrationTest: @Transactional;
    
    @Autowired
    UserSettingDataOnDemand UserSettingIntegrationTest.dod;
    
    @Autowired
    UserSettingService UserSettingIntegrationTest.userSettingService;
    
    @Test
    public void UserSettingIntegrationTest.testCountAllUserSettings() {
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", dod.getRandomUserSetting());
        long count = userSettingService.countAllUserSettings();
        Assert.assertTrue("Counter for 'UserSetting' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserSettingIntegrationTest.testFindUserSetting() {
        UserSetting obj = dod.getRandomUserSetting();
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to provide an identifier", id);
        obj = userSettingService.findUserSetting(id);
        Assert.assertNotNull("Find method for 'UserSetting' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserSetting' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UserSettingIntegrationTest.testFindAllUserSettings() {
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", dod.getRandomUserSetting());
        long count = userSettingService.countAllUserSettings();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserSetting', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserSetting> result = userSettingService.findAllUserSettings();
        Assert.assertNotNull("Find all method for 'UserSetting' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserSetting' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserSettingIntegrationTest.testFindUserSettingEntries() {
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", dod.getRandomUserSetting());
        long count = userSettingService.countAllUserSettings();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserSetting> result = userSettingService.findUserSettingEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserSetting' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserSetting' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserSettingIntegrationTest.testSaveUserSetting() {
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", dod.getRandomUserSetting());
        UserSetting obj = dod.getNewTransientUserSetting(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserSetting' identifier to be null", obj.getId());
        userSettingService.saveUserSetting(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'UserSetting' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UserSettingIntegrationTest.testDeleteUserSetting() {
        UserSetting obj = dod.getRandomUserSetting();
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UserSetting' failed to provide an identifier", id);
        obj = userSettingService.findUserSetting(id);
        userSettingService.deleteUserSetting(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'UserSetting' with identifier '" + id + "'", userSettingService.findUserSetting(id));
    }
    
}
