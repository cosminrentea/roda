// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.PersonLinksDataOnDemand;
import ro.roda.PersonLinksIntegrationTest;
import ro.roda.service.PersonLinksService;

privileged aspect PersonLinksIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersonLinksIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersonLinksIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersonLinksIntegrationTest: @Transactional;
    
    @Autowired
    PersonLinksDataOnDemand PersonLinksIntegrationTest.dod;
    
    @Autowired
    PersonLinksService PersonLinksIntegrationTest.personLinksService;
    
    @Test
    public void PersonLinksIntegrationTest.testCountAllPersonLinkses() {
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", dod.getRandomPersonLinks());
        long count = personLinksService.countAllPersonLinkses();
        Assert.assertTrue("Counter for 'PersonLinks' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersonLinksIntegrationTest.testFindPersonLinks() {
        PersonLinks obj = dod.getRandomPersonLinks();
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to provide an identifier", id);
        obj = personLinksService.findPersonLinks(id);
        Assert.assertNotNull("Find method for 'PersonLinks' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PersonLinks' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersonLinksIntegrationTest.testFindAllPersonLinkses() {
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", dod.getRandomPersonLinks());
        long count = personLinksService.countAllPersonLinkses();
        Assert.assertTrue("Too expensive to perform a find all test for 'PersonLinks', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PersonLinks> result = personLinksService.findAllPersonLinkses();
        Assert.assertNotNull("Find all method for 'PersonLinks' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PersonLinks' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersonLinksIntegrationTest.testFindPersonLinksEntries() {
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", dod.getRandomPersonLinks());
        long count = personLinksService.countAllPersonLinkses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PersonLinks> result = personLinksService.findPersonLinksEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PersonLinks' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PersonLinks' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersonLinksIntegrationTest.testSavePersonLinks() {
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", dod.getRandomPersonLinks());
        PersonLinks obj = dod.getNewTransientPersonLinks(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PersonLinks' identifier to be null", obj.getId());
        personLinksService.savePersonLinks(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'PersonLinks' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersonLinksIntegrationTest.testDeletePersonLinks() {
        PersonLinks obj = dod.getRandomPersonLinks();
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonLinks' failed to provide an identifier", id);
        obj = personLinksService.findPersonLinks(id);
        personLinksService.deletePersonLinks(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'PersonLinks' with identifier '" + id + "'", personLinksService.findPersonLinks(id));
    }
    
}
