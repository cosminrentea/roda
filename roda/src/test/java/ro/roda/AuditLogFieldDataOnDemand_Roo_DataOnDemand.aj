// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ro.roda.AuditLogField;
import ro.roda.AuditLogFieldDataOnDemand;
import ro.roda.AuditLogTable;
import ro.roda.AuditLogTableDataOnDemand;

privileged aspect AuditLogFieldDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AuditLogFieldDataOnDemand: @Component;
    
    private Random AuditLogFieldDataOnDemand.rnd = new SecureRandom();
    
    private List<AuditLogField> AuditLogFieldDataOnDemand.data;
    
    @Autowired
    AuditLogTableDataOnDemand AuditLogFieldDataOnDemand.auditLogTableDataOnDemand;
    
    public AuditLogField AuditLogFieldDataOnDemand.getNewTransientAuditLogField(int index) {
        AuditLogField obj = new AuditLogField();
        setAuditedTable(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void AuditLogFieldDataOnDemand.setAuditedTable(AuditLogField obj, int index) {
        AuditLogTable auditedTable = auditLogTableDataOnDemand.getRandomAuditLogTable();
        obj.setAuditedTable(auditedTable);
    }
    
    public void AuditLogFieldDataOnDemand.setName(AuditLogField obj, int index) {
        String name = "name_" + index;
        if (name.length() > 40) {
            name = name.substring(0, 40);
        }
        obj.setName(name);
    }
    
    public AuditLogField AuditLogFieldDataOnDemand.getSpecificAuditLogField(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AuditLogField obj = data.get(index);
        Integer id = obj.getId();
        return AuditLogField.findAuditLogField(id);
    }
    
    public AuditLogField AuditLogFieldDataOnDemand.getRandomAuditLogField() {
        init();
        AuditLogField obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return AuditLogField.findAuditLogField(id);
    }
    
    public boolean AuditLogFieldDataOnDemand.modifyAuditLogField(AuditLogField obj) {
        return false;
    }
    
    public void AuditLogFieldDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AuditLogField.findAuditLogFieldEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AuditLogField' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AuditLogField>();
        for (int i = 0; i < 10; i++) {
            AuditLogField obj = getNewTransientAuditLogField(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
