// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ro.roda.AuditLogAction;
import ro.roda.AuditLogActionDataOnDemand;
import ro.roda.AuditLogChange;
import ro.roda.AuditLogChangeDataOnDemand;
import ro.roda.AuditLogField;
import ro.roda.AuditLogFieldDataOnDemand;

privileged aspect AuditLogChangeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AuditLogChangeDataOnDemand: @Component;
    
    private Random AuditLogChangeDataOnDemand.rnd = new SecureRandom();
    
    private List<AuditLogChange> AuditLogChangeDataOnDemand.data;
    
    @Autowired
    AuditLogActionDataOnDemand AuditLogChangeDataOnDemand.auditLogActionDataOnDemand;
    
    @Autowired
    AuditLogFieldDataOnDemand AuditLogChangeDataOnDemand.auditLogFieldDataOnDemand;
    
    public AuditLogChange AuditLogChangeDataOnDemand.getNewTransientAuditLogChange(int index) {
        AuditLogChange obj = new AuditLogChange();
        setAction(obj, index);
        setField(obj, index);
        setNewValue(obj, index);
        setOldValue(obj, index);
        return obj;
    }
    
    public void AuditLogChangeDataOnDemand.setAction(AuditLogChange obj, int index) {
        AuditLogAction action = auditLogActionDataOnDemand.getRandomAuditLogAction();
        obj.setAction(action);
    }
    
    public void AuditLogChangeDataOnDemand.setField(AuditLogChange obj, int index) {
        AuditLogField field = auditLogFieldDataOnDemand.getRandomAuditLogField();
        obj.setField(field);
    }
    
    public void AuditLogChangeDataOnDemand.setNewValue(AuditLogChange obj, int index) {
        String newValue = "newValue_" + index;
        if (newValue.length() > 255) {
            newValue = newValue.substring(0, 255);
        }
        obj.setNewValue(newValue);
    }
    
    public void AuditLogChangeDataOnDemand.setOldValue(AuditLogChange obj, int index) {
        String oldValue = "oldValue_" + index;
        if (oldValue.length() > 255) {
            oldValue = oldValue.substring(0, 255);
        }
        obj.setOldValue(oldValue);
    }
    
    public AuditLogChange AuditLogChangeDataOnDemand.getSpecificAuditLogChange(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AuditLogChange obj = data.get(index);
        Integer id = obj.getId();
        return AuditLogChange.findAuditLogChange(id);
    }
    
    public AuditLogChange AuditLogChangeDataOnDemand.getRandomAuditLogChange() {
        init();
        AuditLogChange obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return AuditLogChange.findAuditLogChange(id);
    }
    
    public boolean AuditLogChangeDataOnDemand.modifyAuditLogChange(AuditLogChange obj) {
        return false;
    }
    
    public void AuditLogChangeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AuditLogChange.findAuditLogChangeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AuditLogChange' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AuditLogChange>();
        for (int i = 0; i < 10; i++) {
            AuditLogChange obj = getNewTransientAuditLogChange(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
