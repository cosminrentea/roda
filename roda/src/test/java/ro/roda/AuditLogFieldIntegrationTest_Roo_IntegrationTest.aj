// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.AuditLogField;
import ro.roda.AuditLogFieldDataOnDemand;
import ro.roda.AuditLogFieldIntegrationTest;

privileged aspect AuditLogFieldIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AuditLogFieldIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AuditLogFieldIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AuditLogFieldIntegrationTest: @Transactional;
    
    @Autowired
    AuditLogFieldDataOnDemand AuditLogFieldIntegrationTest.dod;
    
    @Test
    public void AuditLogFieldIntegrationTest.testCountAuditLogFields() {
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", dod.getRandomAuditLogField());
        long count = AuditLogField.countAuditLogFields();
        Assert.assertTrue("Counter for 'AuditLogField' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AuditLogFieldIntegrationTest.testFindAuditLogField() {
        AuditLogField obj = dod.getRandomAuditLogField();
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to provide an identifier", id);
        obj = AuditLogField.findAuditLogField(id);
        Assert.assertNotNull("Find method for 'AuditLogField' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AuditLogField' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AuditLogFieldIntegrationTest.testFindAllAuditLogFields() {
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", dod.getRandomAuditLogField());
        long count = AuditLogField.countAuditLogFields();
        Assert.assertTrue("Too expensive to perform a find all test for 'AuditLogField', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AuditLogField> result = AuditLogField.findAllAuditLogFields();
        Assert.assertNotNull("Find all method for 'AuditLogField' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AuditLogField' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AuditLogFieldIntegrationTest.testFindAuditLogFieldEntries() {
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", dod.getRandomAuditLogField());
        long count = AuditLogField.countAuditLogFields();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AuditLogField> result = AuditLogField.findAuditLogFieldEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AuditLogField' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AuditLogField' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AuditLogFieldIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", dod.getRandomAuditLogField());
        AuditLogField obj = dod.getNewTransientAuditLogField(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AuditLogField' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AuditLogField' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AuditLogFieldIntegrationTest.testRemove() {
        AuditLogField obj = dod.getRandomAuditLogField();
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogField' failed to provide an identifier", id);
        obj = AuditLogField.findAuditLogField(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AuditLogField' with identifier '" + id + "'", AuditLogField.findAuditLogField(id));
    }
    
}
