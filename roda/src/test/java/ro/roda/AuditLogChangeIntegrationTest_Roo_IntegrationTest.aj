// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.AuditLogChange;
import ro.roda.AuditLogChangeDataOnDemand;
import ro.roda.AuditLogChangeIntegrationTest;

privileged aspect AuditLogChangeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AuditLogChangeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AuditLogChangeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AuditLogChangeIntegrationTest: @Transactional;
    
    @Autowired
    AuditLogChangeDataOnDemand AuditLogChangeIntegrationTest.dod;
    
    @Test
    public void AuditLogChangeIntegrationTest.testCountAuditLogChanges() {
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", dod.getRandomAuditLogChange());
        long count = AuditLogChange.countAuditLogChanges();
        Assert.assertTrue("Counter for 'AuditLogChange' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AuditLogChangeIntegrationTest.testFindAuditLogChange() {
        AuditLogChange obj = dod.getRandomAuditLogChange();
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to provide an identifier", id);
        obj = AuditLogChange.findAuditLogChange(id);
        Assert.assertNotNull("Find method for 'AuditLogChange' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AuditLogChange' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AuditLogChangeIntegrationTest.testFindAllAuditLogChanges() {
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", dod.getRandomAuditLogChange());
        long count = AuditLogChange.countAuditLogChanges();
        Assert.assertTrue("Too expensive to perform a find all test for 'AuditLogChange', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AuditLogChange> result = AuditLogChange.findAllAuditLogChanges();
        Assert.assertNotNull("Find all method for 'AuditLogChange' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AuditLogChange' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AuditLogChangeIntegrationTest.testFindAuditLogChangeEntries() {
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", dod.getRandomAuditLogChange());
        long count = AuditLogChange.countAuditLogChanges();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AuditLogChange> result = AuditLogChange.findAuditLogChangeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AuditLogChange' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AuditLogChange' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AuditLogChangeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", dod.getRandomAuditLogChange());
        AuditLogChange obj = dod.getNewTransientAuditLogChange(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AuditLogChange' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AuditLogChange' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AuditLogChangeIntegrationTest.testRemove() {
        AuditLogChange obj = dod.getRandomAuditLogChange();
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogChange' failed to provide an identifier", id);
        obj = AuditLogChange.findAuditLogChange(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AuditLogChange' with identifier '" + id + "'", AuditLogChange.findAuditLogChange(id));
    }
    
}
