// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.SettingGroup;
import ro.roda.SettingGroupDataOnDemand;
import ro.roda.SettingGroupIntegrationTest;

privileged aspect SettingGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SettingGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SettingGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SettingGroupIntegrationTest: @Transactional;
    
    @Autowired
    private SettingGroupDataOnDemand SettingGroupIntegrationTest.dod;
    
    @Test
    public void SettingGroupIntegrationTest.testCountSettingGroups() {
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", dod.getRandomSettingGroup());
        long count = SettingGroup.countSettingGroups();
        Assert.assertTrue("Counter for 'SettingGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SettingGroupIntegrationTest.testFindSettingGroup() {
        SettingGroup obj = dod.getRandomSettingGroup();
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to provide an identifier", id);
        obj = SettingGroup.findSettingGroup(id);
        Assert.assertNotNull("Find method for 'SettingGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SettingGroup' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SettingGroupIntegrationTest.testFindAllSettingGroups() {
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", dod.getRandomSettingGroup());
        long count = SettingGroup.countSettingGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'SettingGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SettingGroup> result = SettingGroup.findAllSettingGroups();
        Assert.assertNotNull("Find all method for 'SettingGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SettingGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SettingGroupIntegrationTest.testFindSettingGroupEntries() {
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", dod.getRandomSettingGroup());
        long count = SettingGroup.countSettingGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SettingGroup> result = SettingGroup.findSettingGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SettingGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SettingGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SettingGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", dod.getRandomSettingGroup());
        SettingGroup obj = dod.getNewTransientSettingGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SettingGroup' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'SettingGroup' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SettingGroupIntegrationTest.testRemove() {
        SettingGroup obj = dod.getRandomSettingGroup();
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SettingGroup' failed to provide an identifier", id);
        obj = SettingGroup.findSettingGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SettingGroup' with identifier '" + id + "'", SettingGroup.findSettingGroup(id));
    }
    
}
