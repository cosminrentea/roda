// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.PersonAddress;
import ro.roda.PersonAddressDataOnDemand;
import ro.roda.PersonAddressIntegrationTest;
import ro.roda.PersonAddressPK;

privileged aspect PersonAddressIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersonAddressIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersonAddressIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersonAddressIntegrationTest: @Transactional;
    
    @Autowired
    private PersonAddressDataOnDemand PersonAddressIntegrationTest.dod;
    
    @Test
    public void PersonAddressIntegrationTest.testCountPersonAddresses() {
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", dod.getRandomPersonAddress());
        long count = PersonAddress.countPersonAddresses();
        Assert.assertTrue("Counter for 'PersonAddress' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersonAddressIntegrationTest.testFindPersonAddress() {
        PersonAddress obj = dod.getRandomPersonAddress();
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", obj);
        PersonAddressPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to provide an identifier", id);
        obj = PersonAddress.findPersonAddress(id);
        Assert.assertNotNull("Find method for 'PersonAddress' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PersonAddress' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersonAddressIntegrationTest.testFindAllPersonAddresses() {
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", dod.getRandomPersonAddress());
        long count = PersonAddress.countPersonAddresses();
        Assert.assertTrue("Too expensive to perform a find all test for 'PersonAddress', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PersonAddress> result = PersonAddress.findAllPersonAddresses();
        Assert.assertNotNull("Find all method for 'PersonAddress' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PersonAddress' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersonAddressIntegrationTest.testFindPersonAddressEntries() {
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", dod.getRandomPersonAddress());
        long count = PersonAddress.countPersonAddresses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PersonAddress> result = PersonAddress.findPersonAddressEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PersonAddress' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PersonAddress' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersonAddressIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", dod.getRandomPersonAddress());
        PersonAddress obj = dod.getNewTransientPersonAddress(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to provide a new transient entity", obj);
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'PersonAddress' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersonAddressIntegrationTest.testRemove() {
        PersonAddress obj = dod.getRandomPersonAddress();
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to initialize correctly", obj);
        PersonAddressPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonAddress' failed to provide an identifier", id);
        obj = PersonAddress.findPersonAddress(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PersonAddress' with identifier '" + id + "'", PersonAddress.findPersonAddress(id));
    }
    
}
