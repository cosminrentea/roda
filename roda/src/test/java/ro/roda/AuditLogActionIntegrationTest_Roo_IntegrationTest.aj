// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.AuditLogAction;
import ro.roda.AuditLogActionDataOnDemand;
import ro.roda.AuditLogActionIntegrationTest;

privileged aspect AuditLogActionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AuditLogActionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AuditLogActionIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AuditLogActionIntegrationTest: @Transactional;
    
    @Autowired
    AuditLogActionDataOnDemand AuditLogActionIntegrationTest.dod;
    
    @Test
    public void AuditLogActionIntegrationTest.testCountAuditLogActions() {
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", dod.getRandomAuditLogAction());
        long count = AuditLogAction.countAuditLogActions();
        Assert.assertTrue("Counter for 'AuditLogAction' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AuditLogActionIntegrationTest.testFindAuditLogAction() {
        AuditLogAction obj = dod.getRandomAuditLogAction();
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to provide an identifier", id);
        obj = AuditLogAction.findAuditLogAction(id);
        Assert.assertNotNull("Find method for 'AuditLogAction' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AuditLogAction' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AuditLogActionIntegrationTest.testFindAllAuditLogActions() {
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", dod.getRandomAuditLogAction());
        long count = AuditLogAction.countAuditLogActions();
        Assert.assertTrue("Too expensive to perform a find all test for 'AuditLogAction', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AuditLogAction> result = AuditLogAction.findAllAuditLogActions();
        Assert.assertNotNull("Find all method for 'AuditLogAction' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AuditLogAction' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AuditLogActionIntegrationTest.testFindAuditLogActionEntries() {
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", dod.getRandomAuditLogAction());
        long count = AuditLogAction.countAuditLogActions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AuditLogAction> result = AuditLogAction.findAuditLogActionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AuditLogAction' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AuditLogAction' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AuditLogActionIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", dod.getRandomAuditLogAction());
        AuditLogAction obj = dod.getNewTransientAuditLogAction(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AuditLogAction' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AuditLogAction' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AuditLogActionIntegrationTest.testRemove() {
        AuditLogAction obj = dod.getRandomAuditLogAction();
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogAction' failed to provide an identifier", id);
        obj = AuditLogAction.findAuditLogAction(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AuditLogAction' with identifier '" + id + "'", AuditLogAction.findAuditLogAction(id));
    }
    
}
