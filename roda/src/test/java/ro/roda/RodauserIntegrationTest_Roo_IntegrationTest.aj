// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.Rodauser;
import ro.roda.RodauserDataOnDemand;
import ro.roda.RodauserIntegrationTest;

privileged aspect RodauserIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RodauserIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RodauserIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RodauserIntegrationTest: @Transactional;
    
    @Autowired
    RodauserDataOnDemand RodauserIntegrationTest.dod;
    
    @Test
    public void RodauserIntegrationTest.testCountRodausers() {
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", dod.getRandomRodauser());
        long count = Rodauser.countRodausers();
        Assert.assertTrue("Counter for 'Rodauser' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RodauserIntegrationTest.testFindRodauser() {
        Rodauser obj = dod.getRandomRodauser();
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to provide an identifier", id);
        obj = Rodauser.findRodauser(id);
        Assert.assertNotNull("Find method for 'Rodauser' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Rodauser' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RodauserIntegrationTest.testFindAllRodausers() {
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", dod.getRandomRodauser());
        long count = Rodauser.countRodausers();
        Assert.assertTrue("Too expensive to perform a find all test for 'Rodauser', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Rodauser> result = Rodauser.findAllRodausers();
        Assert.assertNotNull("Find all method for 'Rodauser' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Rodauser' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RodauserIntegrationTest.testFindRodauserEntries() {
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", dod.getRandomRodauser());
        long count = Rodauser.countRodausers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Rodauser> result = Rodauser.findRodauserEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Rodauser' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Rodauser' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RodauserIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", dod.getRandomRodauser());
        Rodauser obj = dod.getNewTransientRodauser(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Rodauser' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Rodauser' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RodauserIntegrationTest.testRemove() {
        Rodauser obj = dod.getRandomRodauser();
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rodauser' failed to provide an identifier", id);
        obj = Rodauser.findRodauser(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Rodauser' with identifier '" + id + "'", Rodauser.findRodauser(id));
    }
    
}
