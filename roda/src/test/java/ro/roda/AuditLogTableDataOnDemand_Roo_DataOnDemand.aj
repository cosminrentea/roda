// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import ro.roda.AuditLogTable;
import ro.roda.AuditLogTableDataOnDemand;

privileged aspect AuditLogTableDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AuditLogTableDataOnDemand: @Component;
    
    private Random AuditLogTableDataOnDemand.rnd = new SecureRandom();
    
    private List<AuditLogTable> AuditLogTableDataOnDemand.data;
    
    public AuditLogTable AuditLogTableDataOnDemand.getNewTransientAuditLogTable(int index) {
        AuditLogTable obj = new AuditLogTable();
        setName(obj, index);
        return obj;
    }
    
    public void AuditLogTableDataOnDemand.setName(AuditLogTable obj, int index) {
        String name = "name_" + index;
        if (name.length() > 40) {
            name = new Random().nextInt(10) + name.substring(1, 40);
        }
        obj.setName(name);
    }
    
    public AuditLogTable AuditLogTableDataOnDemand.getSpecificAuditLogTable(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AuditLogTable obj = data.get(index);
        Integer id = obj.getId();
        return AuditLogTable.findAuditLogTable(id);
    }
    
    public AuditLogTable AuditLogTableDataOnDemand.getRandomAuditLogTable() {
        init();
        AuditLogTable obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return AuditLogTable.findAuditLogTable(id);
    }
    
    public boolean AuditLogTableDataOnDemand.modifyAuditLogTable(AuditLogTable obj) {
        return false;
    }
    
    public void AuditLogTableDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AuditLogTable.findAuditLogTableEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AuditLogTable' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AuditLogTable>();
        for (int i = 0; i < 10; i++) {
            AuditLogTable obj = getNewTransientAuditLogTable(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
