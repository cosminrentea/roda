// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ro.roda.File;
import ro.roda.FileDataOnDemand;
import ro.roda.service.FileService;

privileged aspect FileDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FileDataOnDemand: @Component;
    
    private Random FileDataOnDemand.rnd = new SecureRandom();
    
    private List<File> FileDataOnDemand.data;
    
    @Autowired
    FileService FileDataOnDemand.fileService;
    
    public File FileDataOnDemand.getNewTransientFile(int index) {
        File obj = new File();
        setDescription(obj, index);
        setFiletypeId(obj, index);
        setName(obj, index);
        setSize(obj, index);
        setTitle(obj, index);
        return obj;
    }
    
    public void FileDataOnDemand.setDescription(File obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void FileDataOnDemand.setFiletypeId(File obj, int index) {
        Integer filetypeId = new Integer(index);
        obj.setFiletypeId(filetypeId);
    }
    
    public void FileDataOnDemand.setName(File obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void FileDataOnDemand.setSize(File obj, int index) {
        Long size = new Integer(index).longValue();
        obj.setSize(size);
    }
    
    public void FileDataOnDemand.setTitle(File obj, int index) {
        String title = "title_" + index;
        obj.setTitle(title);
    }
    
    public File FileDataOnDemand.getSpecificFile(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        File obj = data.get(index);
        Integer id = obj.getId();
        return fileService.findFile(id);
    }
    
    public File FileDataOnDemand.getRandomFile() {
        init();
        File obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return fileService.findFile(id);
    }
    
    public boolean FileDataOnDemand.modifyFile(File obj) {
        return false;
    }
    
    public void FileDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = fileService.findFileEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'File' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<File>();
        for (int i = 0; i < 10; i++) {
            File obj = getNewTransientFile(i);
            try {
                fileService.saveFile(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
