// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ro.roda;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import ro.roda.AuditLogChangeset;
import ro.roda.AuditLogChangesetDataOnDemand;
import ro.roda.AuditLogChangesetIntegrationTest;

privileged aspect AuditLogChangesetIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AuditLogChangesetIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AuditLogChangesetIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AuditLogChangesetIntegrationTest: @Transactional;
    
    @Autowired
    AuditLogChangesetDataOnDemand AuditLogChangesetIntegrationTest.dod;
    
    @Test
    public void AuditLogChangesetIntegrationTest.testCountAuditLogChangesets() {
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", dod.getRandomAuditLogChangeset());
        long count = AuditLogChangeset.countAuditLogChangesets();
        Assert.assertTrue("Counter for 'AuditLogChangeset' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AuditLogChangesetIntegrationTest.testFindAuditLogChangeset() {
        AuditLogChangeset obj = dod.getRandomAuditLogChangeset();
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to provide an identifier", id);
        obj = AuditLogChangeset.findAuditLogChangeset(id);
        Assert.assertNotNull("Find method for 'AuditLogChangeset' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AuditLogChangeset' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AuditLogChangesetIntegrationTest.testFindAllAuditLogChangesets() {
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", dod.getRandomAuditLogChangeset());
        long count = AuditLogChangeset.countAuditLogChangesets();
        Assert.assertTrue("Too expensive to perform a find all test for 'AuditLogChangeset', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AuditLogChangeset> result = AuditLogChangeset.findAllAuditLogChangesets();
        Assert.assertNotNull("Find all method for 'AuditLogChangeset' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AuditLogChangeset' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AuditLogChangesetIntegrationTest.testFindAuditLogChangesetEntries() {
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", dod.getRandomAuditLogChangeset());
        long count = AuditLogChangeset.countAuditLogChangesets();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AuditLogChangeset> result = AuditLogChangeset.findAuditLogChangesetEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AuditLogChangeset' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AuditLogChangeset' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AuditLogChangesetIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", dod.getRandomAuditLogChangeset());
        AuditLogChangeset obj = dod.getNewTransientAuditLogChangeset(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AuditLogChangeset' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AuditLogChangeset' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AuditLogChangesetIntegrationTest.testRemove() {
        AuditLogChangeset obj = dod.getRandomAuditLogChangeset();
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AuditLogChangeset' failed to provide an identifier", id);
        obj = AuditLogChangeset.findAuditLogChangeset(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AuditLogChangeset' with identifier '" + id + "'", AuditLogChangeset.findAuditLogChangeset(id));
    }
    
}
